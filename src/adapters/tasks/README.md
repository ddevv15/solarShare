# Task adapters

This area will connect Trigger.dev tasks to framework independent application services. Task payloads remain versioned and schema validated. PostgreSQL owns transaction safety and outbox state.
